Role & Objective:
Act as an expert Full-Stack Frontend Developer. Build a complete, production-ready, mobile-first web application named "ShiftPro Attendance & Payroll Tracker".

CRITICAL ARCHITECTURE RULE (NO BACKEND): > DO NOT build a Node.js backend, JWT authentication, or a traditional database. The user specifically requested NO LOGIN SYSTEM. The app must be fully client-side. Use React (Vite) + Tailwind CSS + Lucide React. State and data MUST be saved directly to the user's device using Zustand with persist middleware (or IndexedDB) so data remains even if the browser is closed.

Mobile UI & UX Strict Constraints:

The app MUST be 100% responsive and optimized for all iPhones and Android devices.

Keyboard Issue Fix: Ensure inputs are never hidden behind the mobile on-screen keyboard. Use min-h-[100dvh] and ensure scrollable input forms (scroll-into-view behavior on focus).

Design: The UI must be highly colorful, vibrant, modern, and easy to use (glassmorphism cards, bright gradients for states like Present, Absent, Overtime, Night Shift).

Core Features to Build:

1. Global Settings & Configuration Panel (Persistent):

Standard Daily Duty Hours (Dropdown/Input: 8h, 9h, 10h, 12h custom).

Lunch & Tea Break Rules (Frequency and duration deduction).

Base Hourly Rate & Overtime (OT) Multiplier.

Fixed Attendance Bonus threshold (e.g., bonus amount if 26 days worked).

2. Daily Attendance & Shift Logger:

Shift Selector (Morning, General, Night, etc.).

Smart Auto-Calculation: User inputs 'Entry Time' and 'Exit Time'. The app calculates Total Hours, Regular Duty Hours, and OT Hours based on settings.

Manual Override: Input fields for manual adjustment.

Break Trackers: Dropdowns for number of tea/lunch breaks taken.

3. Colorful Dashboard:

Display vibrant summary cards (Total Days Worked, Total Duty Hours, Total OT Hours).

Display Financials (Calculated Regular Pay, OT Pay, Bonus, Total Gross Earnings).

Values must update instantly when new data is logged.

4. Interactive Calendar View:

Build an interactive grid calendar (custom or using a lightweight library).

Dates should have visual indicators (Color dots or backgrounds) for Status: Present, Overtime, Night Duty, Holiday, Absent.

Direct Date Selection: Clicking a date opens a bottom-sheet modal (mobile-friendly) to log or edit that specific day's shift and pay details.

Execution Instructions:
Create the complete folder structure, all React components, the Zustand store for local storage, and the styling. Make sure the UI looks like a premium, native mobile app.